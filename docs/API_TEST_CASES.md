# TaskFlow API 自测用例文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | TaskFlow 任务追踪系统 |
| 文档版本 | 1.0 |
| 创建日期 | 2026-01-20 |
| 基于文档 | API_DOCUMENTATION.md |

---

## 测试环境

- **后端 URL**: `http://localhost:8000/api/v1`
- **默认管理员**: admin@taskflow.com / admin123

---

## 1. 认证模块测试用例 (`/api/v1/auth`)

### TC-AUTH-001: 用户登录（OAuth2 格式）- 正确凭据
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-001 |
| **用例名称** | 用户登录（OAuth2 格式）- 正确凭据 |
| **前置条件** | 管理员账户存在 |
| **请求方式** | POST `/api/v1/auth/login` |
| **Content-Type** | application/x-www-form-urlencoded |
| **请求参数** | `username=admin@taskflow.com&password=admin123` |
| **预期结果** | 返回 200，响应包含 `access_token` 和 `token_type: bearer` |
| **优先级** | 高 |

### TC-AUTH-002: 用户登录（OAuth2 格式）- 错误密码
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-002 |
| **用例名称** | 用户登录（OAuth2 格式）- 错误密码 |
| **前置条件** | 无 |
| **请求方式** | POST `/api/v1/auth/login` |
| **Content-Type** | application/x-www-form-urlencoded |
| **请求参数** | `username=admin@taskflow.com&password=wrongpassword` |
| **预期结果** | 返回 401 Unauthorized |
| **优先级** | 高 |

### TC-AUTH-003: 用户登录（JSON 格式）- 正确凭据
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-003 |
| **用例名称** | 用户登录（JSON 格式）- 正确凭据 |
| **前置条件** | 管理员账户存在 |
| **请求方式** | POST `/api/v1/auth/login/json` |
| **请求体** | `{"email": "admin@taskflow.com", "password": "admin123"}` |
| **预期结果** | 返回 200，响应包含 `access_token` |
| **优先级** | 高 |

### TC-AUTH-004: 获取当前用户信息
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-004 |
| **用例名称** | 获取当前用户信息 |
| **前置条件** | 已登录获取 Token |
| **请求方式** | GET `/api/v1/auth/me` |
| **请求头** | `Authorization: Bearer <token>` |
| **预期结果** | 返回 200，响应包含用户信息（id, username, email, roles, permissions 等） |
| **优先级** | 高 |

### TC-AUTH-005: 无 Token 访问受保护资源
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-005 |
| **用例名称** | 无 Token 访问受保护资源 |
| **前置条件** | 未登录 |
| **请求方式** | GET `/api/v1/auth/me` |
| **请求头** | 无 Authorization |
| **预期结果** | 返回 401 Unauthorized |
| **优先级** | 高 |

### TC-AUTH-006: 用户注册
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-006 |
| **用例名称** | 用户注册 |
| **前置条件** | 用户名和邮箱未被使用 |
| **请求方式** | POST `/api/v1/auth/register` |
| **请求体** | `{"username": "testuser", "email": "test@test.com", "password": "test123456", "display_name": "测试用户"}` |
| **预期结果** | 返回 201，响应包含新用户信息，角色为 ["user"] |
| **优先级** | 高 |

### TC-AUTH-007: 重复注册（应失败）
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-007 |
| **用例名称** | 重复注册（应失败） |
| **前置条件** | 用户已存在 |
| **请求方式** | POST `/api/v1/auth/register` |
| **请求体** | 使用已存在的用户名或邮箱 |
| **预期结果** | 返回 400 Bad Request |
| **优先级** | 中 |

### TC-AUTH-008: 修改密码
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-008 |
| **用例名称** | 修改密码 |
| **前置条件** | 已登录 |
| **请求方式** | POST `/api/v1/auth/change-password` |
| **请求头** | `Authorization: Bearer <token>` |
| **请求体** | `{"old_password": "原密码", "new_password": "新密码"}` |
| **预期结果** | 返回 200，消息为"密码修改成功" |
| **优先级** | 中 |

### TC-AUTH-009: 修改密码 - 原密码错误
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-009 |
| **用例名称** | 修改密码 - 原密码错误 |
| **前置条件** | 已登录 |
| **请求方式** | POST `/api/v1/auth/change-password` |
| **请求体** | `{"old_password": "错误密码", "new_password": "新密码"}` |
| **预期结果** | 返回 400 Bad Request |
| **优先级** | 中 |

---

## 2. 设备模块测试用例 (`/api/v1/devices`)

### TC-DEV-001: 创建设备
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-001 |
| **用例名称** | 创建设备 |
| **前置条件** | 已登录，有 device:create 权限 |
| **请求方式** | POST `/api/v1/devices` |
| **请求体** | `{"name": "TestDevice", "category": "嵌入式室内机", "description": "测试设备"}` |
| **预期结果** | 返回 201，响应包含设备 id, name, category, task_count=0 |
| **优先级** | 高 |

### TC-DEV-002: 创建重复设备（应失败）
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-002 |
| **用例名称** | 创建重复设备（应失败） |
| **前置条件** | 设备名已存在 |
| **请求方式** | POST `/api/v1/devices` |
| **请求体** | 使用已存在的设备名 |
| **预期结果** | 返回 400 Bad Request |
| **优先级** | 中 |

### TC-DEV-003: 获取设备列表
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-003 |
| **用例名称** | 获取设备列表 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/devices` |
| **查询参数** | 可选：page, page_size, category, search |
| **预期结果** | 返回 200，响应包含 items, total, page, page_size, has_next |
| **优先级** | 高 |

### TC-DEV-004: 获取所有设备（不分页）
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-004 |
| **用例名称** | 获取所有设备（不分页） |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/devices/all` |
| **预期结果** | 返回 200，响应为设备对象数组 |
| **优先级** | 中 |

### TC-DEV-005: 获取设备类别列表
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-005 |
| **用例名称** | 获取设备类别列表 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/devices/categories` |
| **预期结果** | 返回 200，响应为类别字符串数组 |
| **优先级** | 中 |

### TC-DEV-006: 获取设备详情
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-006 |
| **用例名称** | 获取设备详情 |
| **前置条件** | 已登录，设备存在 |
| **请求方式** | GET `/api/v1/devices/{device_id}` |
| **预期结果** | 返回 200，响应为完整设备对象 |
| **优先级** | 高 |

### TC-DEV-007: 更新设备
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-007 |
| **用例名称** | 更新设备 |
| **前置条件** | 已登录，有 device:edit 权限 |
| **请求方式** | PUT `/api/v1/devices/{device_id}` |
| **请求体** | `{"description": "更新后的描述"}` |
| **预期结果** | 返回 200，响应为更新后的设备对象 |
| **优先级** | 中 |

### TC-DEV-008: 删除设备
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-008 |
| **用例名称** | 删除设备 |
| **前置条件** | 已登录，有 device:delete 权限 |
| **请求方式** | DELETE `/api/v1/devices/{device_id}` |
| **预期结果** | 返回 200，消息为"设备 'XXX' 已删除" |
| **优先级** | 中 |

### TC-DEV-009: 按类别筛选设备
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-DEV-009 |
| **用例名称** | 按类别筛选设备 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/devices?category=嵌入式室内机` |
| **预期结果** | 返回 200，仅包含该类别的设备 |
| **优先级** | 中 |

---

## 3. 任务模块测试用例 (`/api/v1/tasks`)

### TC-TASK-001: 创建任务
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-001 |
| **用例名称** | 创建任务 |
| **前置条件** | 已登录，有 task:create 权限 |
| **请求方式** | POST `/api/v1/tasks` |
| **请求体** | `{"title": "新任务", "source": "Internal_RD", "description": "任务描述", "priority": 3, "target_date": "2026-02-01", "device_ids": ["uuid"]}` |
| **预期结果** | 返回 201，响应包含 id, task_id, title, status=Backlog, progress=0 |
| **优先级** | 高 |

### TC-TASK-002: 获取任务列表
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-002 |
| **用例名称** | 获取任务列表 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/tasks` |
| **查询参数** | 可选：status, source, device_id, assignee_id, search, page, page_size |
| **预期结果** | 返回 200，响应包含 items, total, page, page_size, has_next |
| **优先级** | 高 |

### TC-TASK-003: 获取任务详情
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-003 |
| **用例名称** | 获取任务详情 |
| **前置条件** | 已登录，任务存在 |
| **请求方式** | GET `/api/v1/tasks/{task_id}` |
| **路径参数** | task_id: UUID 或业务ID (如 TASK-2026-21243A) |
| **预期结果** | 返回 200，响应为完整任务对象 |
| **优先级** | 高 |

### TC-TASK-004: 获取不存在的任务
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-004 |
| **用例名称** | 获取不存在的任务 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/tasks/{不存在的ID}` |
| **预期结果** | 返回 404 Not Found |
| **优先级** | 中 |

### TC-TASK-005: 更新任务
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-005 |
| **用例名称** | 更新任务 |
| **前置条件** | 已登录，有 task:edit 权限 |
| **请求方式** | PUT `/api/v1/tasks/{task_id}` |
| **请求体** | `{"title": "更新后的标题", "priority": 4}` |
| **预期结果** | 返回 200，响应为更新后的任务对象 |
| **优先级** | 高 |

### TC-TASK-006: 更新任务状态 - Backlog → In_Progress
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-006 |
| **用例名称** | 更新任务状态 - Backlog → In_Progress |
| **前置条件** | 任务状态为 Backlog |
| **请求方式** | PUT `/api/v1/tasks/{task_id}/status` |
| **请求体** | `{"status": "In_Progress", "comment": "开始开发"}` |
| **预期结果** | 返回 200，任务状态变为 In_Progress |
| **优先级** | 高 |

### TC-TASK-007: 更新任务状态 - In_Progress → Testing
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-007 |
| **用例名称** | 更新任务状态 - In_Progress → Testing |
| **前置条件** | 任务状态为 In_Progress |
| **请求方式** | PUT `/api/v1/tasks/{task_id}/status` |
| **请求体** | `{"status": "Testing", "comment": "进入测试阶段"}` |
| **预期结果** | 返回 200，任务状态变为 Testing |
| **优先级** | 高 |

### TC-TASK-008: 更新任务状态 - Testing → Closed
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-008 |
| **用例名称** | 更新任务状态 - Testing → Closed |
| **前置条件** | 任务状态为 Testing |
| **请求方式** | PUT `/api/v1/tasks/{task_id}/status` |
| **请求体** | `{"status": "Closed", "actual_date": "2026-01-20", "comment": "任务完成"}` |
| **预期结果** | 返回 200，任务状态变为 Closed，actual_date 被设置 |
| **优先级** | 高 |

### TC-TASK-009: 更新任务进度
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-009 |
| **用例名称** | 更新任务进度 |
| **前置条件** | 已登录，有 task:edit 权限 |
| **请求方式** | PUT `/api/v1/tasks/{task_id}/progress` |
| **请求体** | `{"progress": 50, "comment": "完成50%"}` |
| **预期结果** | 返回 200，任务进度变为 50 |
| **优先级** | 中 |

### TC-TASK-010: 归档任务
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-010 |
| **用例名称** | 归档任务 |
| **前置条件** | 已登录，有 task:archive 权限 |
| **请求方式** | POST `/api/v1/tasks/{task_id}/archive` |
| **预期结果** | 返回 200，任务状态变为 Archived |
| **优先级** | 中 |

### TC-TASK-011: 删除任务
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-011 |
| **用例名称** | 删除任务 |
| **前置条件** | 已登录，有 task:delete 权限 |
| **请求方式** | DELETE `/api/v1/tasks/{task_id}` |
| **预期结果** | 返回 200，消息为"任务 'TASK-XXX' 已删除" |
| **优先级** | 中 |

### TC-TASK-012: 按状态筛选任务
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-012 |
| **用例名称** | 按状态筛选任务 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/tasks?status=Testing` |
| **预期结果** | 返回 200，仅包含 Testing 状态的任务 |
| **优先级** | 中 |

### TC-TASK-013: 搜索任务
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-013 |
| **用例名称** | 搜索任务 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/tasks?search=关键词` |
| **预期结果** | 返回 200，包含匹配关键词的任务 |
| **优先级** | 中 |

### TC-TASK-014: 更新设备状态
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-014 |
| **用例名称** | 更新设备状态 |
| **前置条件** | 任务已关联设备 |
| **请求方式** | POST `/api/v1/tasks/{task_id}/devices/{device_id}/status?new_status=in_progress` |
| **预期结果** | 返回 200，设备状态变更 |
| **优先级** | 中 |

### TC-TASK-015: 添加禅道关联
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-015 |
| **用例名称** | 添加禅道关联 |
| **前置条件** | 任务已关联设备 |
| **请求方式** | POST `/api/v1/tasks/{task_id}/devices/{device_id}/zentao?zentao_type=story&zentao_id=12345` |
| **预期结果** | 返回 200，设备关联禅道信息 |
| **优先级** | 中 |

### TC-TASK-016: 删除禅道关联
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-TASK-016 |
| **用例名称** | 删除禅道关联 |
| **前置条件** | 设备已有禅道关联 |
| **请求方式** | DELETE `/api/v1/tasks/{task_id}/devices/{device_id}/zentao` |
| **预期结果** | 返回 200，禅道关联被删除 |
| **优先级** | 低 |

---

## 4. 用户管理模块测试用例 (`/api/v1/users`)

### TC-USER-001: 获取用户列表
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-001 |
| **用例名称** | 获取用户列表 |
| **前置条件** | 已登录，有 user:view 权限 |
| **请求方式** | GET `/api/v1/users` |
| **查询参数** | 可选：page, page_size, search, department, is_active |
| **预期结果** | 返回 200，响应包含 items, total, page, page_size, has_next |
| **优先级** | 高 |

### TC-USER-002: 获取用户详情
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-002 |
| **用例名称** | 获取用户详情 |
| **前置条件** | 已登录，用户存在 |
| **请求方式** | GET `/api/v1/users/{user_id}` |
| **预期结果** | 返回 200，响应为完整用户对象 |
| **优先级** | 高 |

### TC-USER-003: 创建用户
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-003 |
| **用例名称** | 创建用户 |
| **前置条件** | 已登录，有 user:create 权限 |
| **请求方式** | POST `/api/v1/users` |
| **请求体** | `{"username": "newuser", "email": "new@test.com", "password": "test123", "display_name": "新用户", "department": "测试部门"}` |
| **预期结果** | 返回 201，响应为新用户对象 |
| **优先级** | 中 |

### TC-USER-004: 更新用户
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-004 |
| **用例名称** | 更新用户 |
| **前置条件** | 已登录，有 user:edit 权限 |
| **请求方式** | PUT `/api/v1/users/{user_id}` |
| **请求体** | `{"display_name": "更新后的显示名", "department": "研发部"}` |
| **预期结果** | 返回 200，响应为更新后的用户对象 |
| **优先级** | 中 |

### TC-USER-005: 删除用户
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-005 |
| **用例名称** | 删除用户 |
| **前置条件** | 已登录，有 user:delete 权限，删除非自己 |
| **请求方式** | DELETE `/api/v1/users/{user_id}` |
| **预期结果** | 返回 200，消息为"用户已删除" |
| **优先级** | 中 |

### TC-USER-006: 删除自己（应失败）
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-006 |
| **用例名称** | 删除自己（应失败） |
| **前置条件** | 已登录 |
| **请求方式** | DELETE `/api/v1/users/{当前用户ID}` |
| **预期结果** | 返回 400 Bad Request |
| **优先级** | 中 |

### TC-USER-007: 按部门筛选用户
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-007 |
| **用例名称** | 按部门筛选用户 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/users?department=研发部` |
| **预期结果** | 返回 200，仅包含该部门的用户 |
| **优先级** | 低 |

### TC-USER-008: 获取角色列表
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-008 |
| **用例名称** | 获取角色列表 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/users/roles/list` |
| **预期结果** | 返回 200，响应包含 items（角色数组）和 total |
| **优先级** | 中 |

### TC-USER-009: 创建角色
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-009 |
| **用例名称** | 创建角色 |
| **前置条件** | 已登录，是超级管理员 |
| **请求方式** | POST `/api/v1/users/roles` |
| **请求体** | `{"code": "test_role", "name": "测试角色", "description": "测试角色描述", "permission_codes": ["task:view", "task:edit"]}` |
| **预期结果** | 返回 201，响应为新角色对象 |
| **优先级** | 中 |

### TC-USER-010: 普通用户创建角色（应失败）
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-010 |
| **用例名称** | 普通用户创建角色（应失败） |
| **前置条件** | 已登录，非超级管理员 |
| **请求方式** | POST `/api/v1/users/roles` |
| **预期结果** | 返回 403 Forbidden |
| **优先级** | 中 |

### TC-USER-011: 更新角色
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-011 |
| **用例名称** | 更新角色 |
| **前置条件** | 已登录，是超级管理员 |
| **请求方式** | PUT `/api/v1/users/roles/{role_id}` |
| **请求体** | `{"name": "更新后的角色名", "permission_codes": ["task:view"]}` |
| **预期结果** | 返回 200，响应为更新后的角色对象 |
| **优先级** | 中 |

### TC-USER-012: 删除角色
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-012 |
| **用例名称** | 删除角色 |
| **前置条件** | 已登录，是超级管理员，角色非系统角色 |
| **请求方式** | DELETE `/api/v1/users/roles/{role_id}` |
| **预期结果** | 返回 200，消息为"角色已删除" |
| **优先级** | 低 |

### TC-USER-013: 删除系统角色（应失败）
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-013 |
| **用例名称** | 删除系统角色（应失败） |
| **前置条件** | 已登录，是超级管理员 |
| **请求方式** | DELETE `/api/v1/users/roles/{系统角色ID}` |
| **预期结果** | 返回 400 Bad Request |
| **优先级** | 中 |

### TC-USER-014: 分配用户角色
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-014 |
| **用例名称** | 分配用户角色 |
| **前置条件** | 已登录，有 user:assign_role 权限 |
| **请求方式** | PUT `/api/v1/users/{user_id}/roles` |
| **请求体** | `["role_id_1", "role_id_2"]` |
| **预期结果** | 返回 200，响应为更新后的用户对象 |
| **优先级** | 中 |

### TC-USER-015: 获取权限列表
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-USER-015 |
| **用例名称** | 获取权限列表 |
| **前置条件** | 已登录 |
| **请求方式** | GET `/api/v1/users/permissions/list` |
| **预期结果** | 返回 200，响应为权限对象数组 |
| **优先级** | 低 |

---

## 5. 测试用例统计

| 模块 | 用例数量 | 优先级分布 |
|------|----------|-----------|
| 认证模块 | 9 | 高: 5, 中: 4 |
| 设备模块 | 9 | 高: 3, 中: 6 |
| 任务模块 | 16 | 高: 8, 中: 7, 低: 1 |
| 用户管理模块 | 15 | 高: 2, 中: 11, 低: 2 |
| **总计** | **49** | **高: 18, 中: 28, 低: 3** |

---

## 6. 测试流程建议

### 6.1 完整测试流程

1. **准备阶段**
   - 启动后端服务 (localhost:8000)
   - 确保数据库已初始化
   - 确认管理员账户可用

2. **认证测试**
   - 执行 TC-AUTH-001 ~ TC-AUTH-009

3. **设备测试**  
   - 执行 TC-DEV-001 ~ TC-DEV-009
   - 记录创建的测试设备 ID

4. **任务测试**
   - 执行 TC-TASK-001 ~ TC-TASK-016
   - 使用步骤3中创建的设备

5. **用户管理测试**
   - 执行 TC-USER-001 ~ TC-USER-015

6. **清理阶段**
   - 删除测试创建的任务、设备、用户、角色

### 6.2 快速回归测试

仅执行优先级为"高"的 18 个用例：
- TC-AUTH-001, TC-AUTH-002, TC-AUTH-003, TC-AUTH-004, TC-AUTH-005, TC-AUTH-006
- TC-DEV-001, TC-DEV-003, TC-DEV-006
- TC-TASK-001, TC-TASK-002, TC-TASK-003, TC-TASK-005, TC-TASK-006, TC-TASK-007, TC-TASK-008
- TC-USER-001, TC-USER-002

---

**文档结束**
